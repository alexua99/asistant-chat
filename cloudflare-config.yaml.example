# Cloudflare Tunnel Configuration Example
# Скопируйте этот файл в cloudflare-config.yaml и заполните своими данными

tunnel: YOUR_TUNNEL_ID
credentials-file: /home/USERNAME/.cloudflared/YOUR_TUNNEL_ID.json

ingress:
  # Перенаправление основного домена на локальный сервер
  - hostname: your-domain.com
    service: http://localhost:3000
  
  # Альтернативный вариант с www поддоменом
  - hostname: www.your-domain.com
    service: http://localhost:3000
  
  # Catch-all правило (должно быть последним)
  - service: http_status:404

